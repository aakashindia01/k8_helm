apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-angular-app-service
spec:
  selector:
    app: angular-app
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
